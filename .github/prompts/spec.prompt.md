---
name: "spec"
description: "Formalize requirements into a validated, traceable spec before build"
---
Load the `spec` skill via the skill tool and follow its full workflow: Capture → Structure → Validate → Transfer, with the four validation gates and traceability from need to build.