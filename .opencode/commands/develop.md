---
description: "Default baseline for normal software work — small safe iterations"
---
Load the `develop` skill via the skill tool and follow its default baseline workflow: inspect, create, test, review, continue with small safe iterations and built-in validation.

Apply it to: $ARGUMENTS