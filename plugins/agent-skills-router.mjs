// Compatibility entrypoint for direct OpenCode plugin configurations.
// The installed package uses separate server and TUI entrypoints.

export { AgentSkillsRouter, default } from "./agent-skills-router/server.mjs"
